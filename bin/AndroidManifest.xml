<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="usa.hosmingsystem"
    android:versionCode="1"
    android:versionName="1.0" 
    >
	
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="usa.hosmingsystem.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="usa.hosmingsystem.Forecast"
            android:label="@string/title_activity_forecast" >
        </activity>
        <activity
            android:name="usa.hosmingsystem.Measure"
            android:label="@string/title_activity_measure" >
        </activity>
        <activity
            android:name="usa.hosmingsystem.AsyncTaskTestActivity"
            android:label="@string/title_activity_async_task_test" >
        </activity>
    </application>

</manifest>
